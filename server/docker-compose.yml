version: "3.7"
services:
  database:
    image: mongo
    restart: always
    env_file:
      - ./.server-env
    ports:
      - 27017:27017
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - ../database/init.js:/docker-entrypoint-initdb.d/init.js:ro
