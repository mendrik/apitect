import React from 'react'

import { Checkbox } from '../../../forms/Checkbox'

const Object = () => {
  return <Checkbox name="apiEndpoint" label={'modals.nodeSettings.object.apiEndpoint'} />
}

export default Object
