import React from 'react'
import { useTranslation } from 'react-i18next'

import { SettingsProps } from '../NodeSettings'

const Object: SettingsProps = ({ close }) => {
  const { t } = useTranslation()

  return <div>Object</div>
}

export default Object
