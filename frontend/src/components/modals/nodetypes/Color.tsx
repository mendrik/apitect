import React from 'react'
import { useTranslation } from 'react-i18next'

import { SettingsProps } from '../NodeSettings'

const Color: SettingsProps = ({ close }) => {
  const { t } = useTranslation()

  return <div>Color</div>
}

export default Color
